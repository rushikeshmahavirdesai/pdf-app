# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: url-to-pdf
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
